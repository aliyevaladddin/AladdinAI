name = "aladdinai"
compatibility_date = "2024-01-01"

# Custom functions directory (default is /functions)
[build]
command = ""

[build.upload]
format = "service-worker"

# Point to .github/functions instead of /functions
pages_build_output_dir = "."

# Environment variables are configured in Cloudflare Dashboard
# See .github/functions/.env.example for required variables
